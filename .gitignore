# Earlier résumé versions, kept locally for reference only.
# These must never reach the web server: they are superseded, and the cv_*
# files are print layouts that hardcode absolute /tmp font paths.
index_old.html
index_prev.html
cv_community_manager.html
cv_developer.html
cv_localizer.html

# Master artwork for the avatar: 2620x2483 RGBA, 9.3 MB. Never served -- the
# deployed avatar.jpg is generated from it. Keep it locally, never upload it.
avatar.png

# Local build/preview leftovers
*.log
*.tmp

# OS and editor noise
Desktop.ini
Thumbs.db
ehthumbs.db
$RECYCLE.BIN/
.DS_Store
._*
*.swp
*~
.idea/
.vscode/
