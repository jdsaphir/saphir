# The server is Linux and Apache can misparse CRLF inside .htaccess, so keep
# every text file LF in the repository and on checkout, including on Windows.
* text=auto eol=lf

*.jpg binary
*.jpeg binary
*.png binary
*.ico binary
*.woff binary
*.woff2 binary
